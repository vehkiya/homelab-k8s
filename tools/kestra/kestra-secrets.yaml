apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kestra-secret
  namespace: tools
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vaultwarden-notes
    kind: ClusterSecretStore
  target:
    name: kestra-secret
    creationPolicy: Owner
  data:
    - secretKey: application-k8s.yml
      remoteRef:
        key: "f980bb7d-a8cc-4c04-ba2f-3d8b696df896"
        property: "kestra-config.yaml"
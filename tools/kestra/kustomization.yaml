apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tools
  
helmCharts:
  - name: kestra
    repo: https://helm.kestra.io/
    version: 0.24.0
    releaseName: kestra
    namespace: tools
    valuesFile: "config/config.yaml"

resources:
  - kestra-secrets.yaml
  - kestra-http-route.yaml
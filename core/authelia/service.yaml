# authelia/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: authelia
  namespace: core
  labels:
    app: authelia
spec:
  type: LoadBalancer
  ports:
    - port: 9091
      targetPort: http
      protocol: TCP
      name: http
  externalTrafficPolicy: Local
  selector:
    app: authelia
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lldap-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: ClusterSecretStore
  target:
    name: lldap-secrets
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        LLDAP_DATABASE_URL: "postgres://{{ .postgres_user }}:{{ .postgres_password }}@{{ .postgres_host }}:{{ .postgres_port }}/{{ .postgres_db }}"
        LLDAP_JWT_SECRET: "{{ .jwt_secret }}"
        LLDAP_LDAP_USER_DN: "{{ .ldap_admin_user }}"
        LLDAP_LDAP_USER_PASS: "{{ .ldap_admin_password }}"
  data:
    - secretKey: postgres_user
      remoteRef:
        key: security/lldap
        property: "postgres_user"
    - secretKey: postgres_password
      remoteRef:
        key: security/lldap
        property: "postgres_password"
    - secretKey: postgres_host
      remoteRef:
        key: security/lldap
        property: "postgres_host"
    - secretKey: postgres_port
      remoteRef:
        key: security/lldap
        property: "postgres_port"
    - secretKey: postgres_db
      remoteRef:
        key: security/lldap
        property: "postgres_db"
    - secretKey: jwt_secret
      remoteRef:
        key: security/lldap
        property: "JWT_SECRET"
    - secretKey: ldap_admin_user
      remoteRef:
        key: security/lldap
        property: "admin_user"
    - secretKey: ldap_admin_password
      remoteRef:
        key: security/lldap
        property: "admin_password"

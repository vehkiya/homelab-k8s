apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap-tls
  namespace: security
spec:
  secretName: lldap-tls-cert
  dnsNames:
    - lldap.kerrlab.app
  issuerRef:
    name: zerossl-prod
    kind: ClusterIssuer

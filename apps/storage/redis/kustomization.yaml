apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: redis
helmCharts:
  - name: redis
    repo: oci://registry-1.docker.io/cloudpirates/redis
    version: 0.2.0
    releaseName: redis
    valuesFile: values/values.yaml
    includeCRDs: true
resources:
  - redis-secret.yaml
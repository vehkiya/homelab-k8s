apiVersion: cilium.io/v2
kind: CiliumBGPPeerConfig
metadata:
  name: ubiquiti-gateway-peer # Name referenced in CiliumBGPNodeConfig
  namespace: kube-system
spec:
  ebgpMultihop: 1
  timers:
    connectRetryTimeSeconds: 120
    holdTimeSeconds: 90
    keepAliveTimeSeconds: 30
  families:
    - afi: ipv4
      safi: unicast
      advertisements:
        matchLabels:
          advertise: "bgp"
---
apiVersion: cilium.io/v2
kind: CiliumBGPClusterConfig
metadata:
  name: default-bgp-cluster-config
  namespace: kube-system
spec:
  bgpInstances:
    - name: homelab-router # An identifier for this BGP instance
      localASN: 65512
      peers:
        - name: "ubiquiti-gateway-peer"
          peerAddress: "10.10.1.1"
          peerASN: 65000
          peerConfigRef:
            name: ubiquiti-gateway-peer
--- # === Internal Traefik Advertisement ===
apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: internal-traefik-lb-advertisement
  namespace: traefik
  labels:
    advertise: "bgp"
spec:
  advertisements:
    - advertisementType: "Service"
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchLabels:
          pool: internal-traefik-lb-pool
--- # === Traefik DMZ Advertisement ===
apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: traefik-dmz-lb-advertisement
  namespace: traefik
  labels:
    advertise: "bgp"
spec:
  advertisements:
    - advertisementType: "Service"
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchLabels:
          pool: traefik-dmz-pool
--- # === General DMZ Advertisement ===
apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: dmz-lb-advertisement
  namespace: traefik
  labels:
    advertise: "bgp"
spec:
  advertisements:
    - advertisementType: "Service"
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchLabels:
          pool: dmz-lb-pool
--- # === General LB Advertisement ===
apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: general-lb-advertisement
  namespace: kube-system
  labels:
    advertise: "bgp"
spec:
  advertisements:
    - advertisementType: Service
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchExpressions:
          - { key: pool, operator: NotIn, values: [ 'internal-traefik-lb-pool', 'traefik-dmz-pool', 'dmz-lb-pool' ] }          
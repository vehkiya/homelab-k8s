apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao-store
  target:
    name: argocd-secret
    creationPolicy: Owner
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: argocd-secret
          app.kubernetes.io/part-of: argocd
      data:
        admin.password: "{{ .password }}"
        admin.passwordMtime: "2025-09-04T23:18:59Z"
        server.secretkey: "{{ .server_key }}"
  data:
    - secretKey: password
      remoteRef:
        key: argocd/admin
        property: password_encrypted
    - secretKey: server_key
      remoteRef:
        key: argocd/admin
        property: server_secretkey
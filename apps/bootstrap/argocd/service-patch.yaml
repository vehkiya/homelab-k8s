apiVersion: v1
kind: Service
metadata:
  name: argocd-server
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
      appProtocol: kubernetes.io/h2c
    - name: https
      port: 443
      protocol: TCP
      targetPort: 8080

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gluetun-protonvpn-secret
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vaultwarden-fields
    kind: ClusterSecretStore
  target:
    name: gluetun-secret
    creationPolicy: Owner
  data:
    - secretKey: WIREGUARD_PRIVATE_KEY
      remoteRef:
        key: 5b79147c-2173-45ff-84d9-b55e0ae43d03
        property: WIREGUARD_PRIVATE_KEY

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: seerr
  name: seerr-config-longhorn-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn-retain
  volumeMode: Filesystem

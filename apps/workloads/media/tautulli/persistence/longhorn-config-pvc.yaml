apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: tautulli
  name: tautulli-config-longhorn-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-retain
  volumeMode: Filesystem

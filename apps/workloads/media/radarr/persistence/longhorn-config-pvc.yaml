apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: radarr
  name: radarr-config-longhorn-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: longhorn
  volumeMode: Filesystem

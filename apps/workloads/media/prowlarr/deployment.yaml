apiVersion: apps/v1
kind: Deployment
metadata:
  name: prowlarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: prowlarr
  template:
    metadata:
      labels:
        app: prowlarr
        networking/expose-web-ui: "true"
    spec:
      initContainers:
        - name: inject-secrets
          image: hairyhenderson/gomplate
          command: [ "gomplate", "-f", "/template/config.xml", "-o", "/config/config.xml" ]
          envFrom:
            - secretRef:
                name: prowlarr-secrets
          volumeMounts:
            - name: prowlarr-template-vol
              mountPath: /template
            # Mount the Longhorn volume into the init container
            - name: prowlarr-config-pvc
              mountPath: /config
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "50m"
              memory: "32Mi"
      containers:
        - name: prowlarr
          image: ghcr.io/home-operations/prowlarr
          env:
            - name: PGID
              value: "100"
            - name: PUID
              value: "1027"
            - name: TZ
              value: "Europe/Chisinau"
          ports:
            - name: web-ui
              containerPort: 9696
              protocol: TCP
          startupProbe:
            httpGet:
              path: /ping
              port: web-ui
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ping
              port: web-ui
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /ping
              port: web-ui
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          resources:
            requests:
              cpu: "10m"
              memory: "50Mi"
            limits:
              cpu: "250m"
              memory: "250Mi"
          volumeMounts:
            - name: prowlarr-config-pvc
              mountPath: /config
          securityContext:
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
      securityContext:
        fsGroupChangePolicy: OnRootMismatch
        fsGroup: 100
        runAsGroup: 100
        runAsNonRoot: true
        runAsUser: 1027
      volumes:
        - name: prowlarr-template-vol
          configMap:
            name: prowlarr-config-template
        - name: prowlarr-config-pvc
          persistentVolumeClaim:
            claimName: prowlarr-config-longhorn-pvc

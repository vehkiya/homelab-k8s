---
apiVersion: v1
kind: Service
metadata:
  name: keeper
  labels:
    app: keeper
spec:
  type: ClusterIP
  ports:
    - name: web-ui
      port: 3000
      targetPort: web-ui
      protocol: TCP
  selector:
    app: keeper

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keeper-data-pvc
  labels:
    app: keeper
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn-retain
  volumeMode: Filesystem

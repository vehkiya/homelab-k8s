apiVersion: v1
kind: Service
metadata:
  name: whoami
spec:
  ipFamilyPolicy: PreferDualStack
  ipFamilies:
    - IPv4
    - IPv6
  selector:
    app: whoami
  ports:
    - port: 80
      name: web-ui

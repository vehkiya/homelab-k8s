apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: excalidraw-policy
spec:
  endpointSelector:
    matchLabels:
      app: excalidraw

  # Allow Ingress (Inbound)
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: keda
            app.kubernetes.io/component: interceptor
      toPorts:
        - ports:
            - port: "80"
              protocol: "TCP"

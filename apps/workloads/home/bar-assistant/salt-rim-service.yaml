apiVersion: v1
kind: Service
metadata:
  name: salt-rim
  labels:
    app: salt-rim
spec:
  selector:
    app: salt-rim
  ports:
    - protocol: TCP
      port: 8080
      name: web-ui
  type: ClusterIP

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: minecraft-vanilla-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao-store
    kind: ClusterSecretStore
  target:
    name: minecraft-vanilla-secret
    creationPolicy: Owner
  data:
    - secretKey: RCON_PASSWORD
      remoteRef:
        key: games/minecraft-vanilla
        property: RCON_PASSWORD
apiVersion: v1
kind: Service
metadata:
  name: media-webhook-notifier
  labels:
    app.kubernetes.io/name: media-webhook-notifier
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: media-webhook-notifier
  ports:
    - name: http-api
      port: 8080
      targetPort: http-api
      protocol: TCP

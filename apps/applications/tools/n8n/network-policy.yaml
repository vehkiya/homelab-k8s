apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: n8n-to-redis
  namespace: redis
spec:
  endpointSelector:
    matchLabels:
      app: redis
  ingress:
    - fromEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": "default"
            "k8s:app": n8n
      toPorts:
        - ports:
            - port: "6379"
              protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: iconify
  namespace: tools
  labels:
    app: iconify
spec:
  selector:
    app: iconify
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP

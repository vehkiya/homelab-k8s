apiVersion: http.keda.sh/v1alpha1
kind: HTTPScaledObject
metadata:
  name: speedtest
  namespace: default
spec:
  hosts:
    - speed.kerrlab.app
  scaleTargetRef:
    name: speedtest
    kind: Deployment
    apiVersion: apps/v1
    service: speedtest-ui
    port: 3000
  replicas:
    min: 0
    max: 1

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-data-pvc
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
---
# === Backup ===
apiVersion: v1
kind: PersistentVolume
metadata:
  name: minecraft-backup-nfs
  labels:
    pv-name: minecraft-backup-nfs
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - nfsvers=4.1
    - async
    - noatime
    - rsize=1048576
    - wsize=1048576
    - soft
    - timeo=30    # 3 second timeout
    - retrans=3         # Retry 3 times then give up
  nfs:
    server: fortress.kerrlab.app
    path: /volume1/cluster/minecraft/base/backups #unique path
  storageClassName: synostorage-nfs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-backup-pvc
  namespace: games
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  selector:
    matchLabels:
      pv-name: minecraft-backup-nfs
  storageClassName: synostorage-nfs
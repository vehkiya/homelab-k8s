apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nodecast-secrets
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vaultwarden-fields
    kind: ClusterSecretStore
  target:
    name: nodecast-secrets
    creationPolicy: Owner
  data:
    - secretKey: OIDC_CLIENT_SECRET
      remoteRef:
        key: "0fbccf24-38d8-44fe-9b27-2ab3cfff5a25"
        property: "OIDC_CLIENT_SECRET"

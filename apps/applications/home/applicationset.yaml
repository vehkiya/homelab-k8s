apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: home-applications
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: bar-assistant
          - name: mealie
  template:
    metadata:
      name: '{{name}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/vehkiya/homelab-k8s.git
        targetRevision: HEAD
        path: 'apps/applications/home/{{name}}'
      destination:
        server: https://kube.kerrlab.app:6443
        namespace: home
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
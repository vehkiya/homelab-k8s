apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: iot
spec:
  selector:
    app: home-assistant # select pods that match this label
  ports:
    - port: 8123
      name: home-assistant-web
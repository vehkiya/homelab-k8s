apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iot

labels:
  - pairs:
      app.kubernetes.io/name: home-assistant

resources:
  - nad.yaml
  - deployment.yaml
  - service.yaml
  - http-route.yaml
  - go2rtc-config.yaml
  - go2rtc-service.yaml
  - go2rtc-http-route.yaml

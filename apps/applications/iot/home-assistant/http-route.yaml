apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hass-route
  namespace: iot
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - "hass.kerrlab.app"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: home-assistant
          port: 8123
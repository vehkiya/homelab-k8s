apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: tailscale-operator
    repo: https://pkgs.tailscale.com/helmcharts
    version: 1.84.2
    releaseName: tailscale-operator
    namespace: tailscale

resources:
  - tailscale-operator-oauth-key.yaml
  - tailscale-key.yaml
apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  namespace: home-assistant
spec:
  selector:
    app: home-assistant # select pods that match this label
  ports:
    - port: 8123
      name: home-assistant-web
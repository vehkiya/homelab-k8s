apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# List of resources to include
resources:
  - ../base
  - unpackerr-app-config.yaml
  - unpackerr-app-secrets.yaml
  - unpackerr-deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/name: unpackerr

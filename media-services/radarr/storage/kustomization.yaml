apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app: radarr
      
      
# Inherit from the base configuration
resources:
  - ../../../base/storage

patches:
  - target:
      kind: PersistentVolume
      name: placeholder-pv # Target the base PV by name
    patch: |-
      # --- Start of PV JSON Patch ---
      - op: replace
        path: /metadata/name
        value: radarr-config-pv
      - op: replace
        path: /spec/capacity/storage
        value: 4Gi
      - op: replace
        path: /spec/csi/volumeHandle
        value: 49b14411-b38b-43ba-a3ba-eeb28205a32f
      # --- End of PV JSON Patch ---

  - target:
      kind: PersistentVolumeClaim
      name: placeholder-pvc # Target the base PVC by name
    patch: |-
      # --- Start of PVC JSON Patch ---
      - op: replace
        path: /metadata/name
        value: radarr-config-pvc
      - op: replace
        path: /metadata/namespace
        value: media
      - op: replace
        path: /spec/resources/requests/storage
        value: 4Gi
      - op: replace
        path: /spec/volumeName
        value: radarr-config-pv # Match the final patched PV name
      # --- End of PVC JSON Patch ---
      
# ConfigMap for non-sensitive configuration
apiVersion: v1
kind: ConfigMap
metadata:
    name: mealie-config
data:
    BASE_URL: https://recipes.kerrlab.app
    ALLOW_SIGNUP: "true"
    DB_ENGINE: postgres
    PUID: "1026"
    PGID: "100"

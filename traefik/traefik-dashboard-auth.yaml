apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: traefik-dashboard-auth-secret
  namespace: traefik
spec:
  target:
    name: traefik-dashboard-auth-secret
  
  secretStoreRef:
    name: vaultwarden-store
    kind: ClusterSecretStore

  data:
    # ---- Radarr ----
    - secretKey: username
      remoteRef:
        key: "eef82d3f-39ef-4e6f-bff2-d2dd9290810f"
        property: "username"
    # ---- Sonarr ----
    - secretKey: password
      remoteRef:
        key: "eef82d3f-39ef-4e6f-bff2-d2dd9290810f"
        property: "password"
# kustomize-pv/base/pv-pvc-base.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  # Kustomize will merge annotations if specified in patches
  annotations:
    pv.kubernetes.io/provisioned-by: csi.san.synology.com
  name: placeholder-pv # Base name, will be overridden by overlay
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi # Placeholder storage, will be overridden
  csi:
    driver: csi.san.synology.com
    volumeHandle: placeholder-uuid # Placeholder handle, will be overridden
    fsType: btrfs # Assuming this is constant
    volumeAttributes: # Assuming these are constant
      dsm: 'fortress.kerrlab.app'
      protocol: 'iscsi'
  persistentVolumeReclaimPolicy: Retain
  storageClassName: synology-iscsi-persistent # Assuming this is constant
  volumeMode: Filesystem
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: placeholder-pvc   # Base name, will be overridden by overlay
  namespace: default      # Base namespace, will be overridden
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi # Placeholder storage, will be overridden
  # IMPORTANT: This needs to match the *final* PV name after patching
  volumeName: placeholder-pv # Base name, must be overridden by patch
  storageClassName: synology-iscsi-persistent # Assuming this is constant
#apiVersion: v1
#kind: Pod
#metadata:
#  name: dns-test
#  namespace: home-assistant
#spec:
#  containers:
#    - name: busybox
#      image: busybox:latest
#      command: [ 'sh', '-c', 'sleep 3600' ] # Keep the pod running
#      volumeMounts:
#        - name: test
#          mountPath: /test
#
#        - name: hass-config-mount
#          subPath: matter-server
#          mountPath: /data  
#  volumes:
#    - name: test
#      persistentVolumeClaim:
#        claimName: matter-data-pvc
#
#    - name: hass-config-mount
#      persistentVolumeClaim:
#        claimName: home-assistant-config-pvc        
##---
##apiVersion: v1
##kind: PersistentVolumeClaim
##metadata:
##  name: test-pvc
##  namespace: default # or the namespace where your application runs
##spec:
##  accessModes:
##    - ReadWriteOnce # Must match the PV's access modes
##  resources:
##    requests:
##      storage: 1Gi # Must be less than or equal to the PV's capacity
##  storageClassName: synology-iscsi-persistent
apiVersion: v1
kind: Service
metadata:
  name: ip-reclaim-test
spec:
  clusterIP: 10.96.0.10
  ports:
    - protocol: TCP
      port: 53
      targetPort: 53